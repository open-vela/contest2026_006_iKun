# 项目检查报告

时间：2026-09-01 18:37

## 一、修改内容

无修改。当前课表页面返回刷新逻辑已正确实现。

## 二、问题原因分析

当前代码**已经正确处理**了课表页面返回后的刷新问题。

## 三、修改文件

无修改文件。

## 四、课表刷新流程分析

### 当前已实现的流程

```
设置页面保存
  ↓
Storage 更新 (campus_semester_start)
  ↓
返回课表页面
  ↓
onShow() 触发
  ↓
ensureStorageReady()
  ↓
refreshSemesterStartFromStorage()  ← 重新读取学期第一周设置
  ↓
refreshCoursesFromStorage()        ← 重新读取课程数据
  ↓
calculateCurrentWeek(now)          ← 重新计算当前教学周
  ↓
selectedWeek 赋值                  ← 更新选中周
  ↓
updateWeekDates(now)               ← 更新日期显示
  ↓
loadTodayCourses(now)              ← 刷新今日课程
  ↓
loadWeekCourses(now)               ← 刷新本周课程
  ↓
loadAllCourses()                   ← 刷新全部课程
```

### 关键代码分析

#### onShow() 函数（第591-610行）

```javascript
onShow() {
  ensureStorageReady()
    .then(() => Promise.all([
      refreshCoursesFromStorage(),
      refreshSemesterStartFromStorage()  // ← 重新读取学期设置
    ]))
    .then(() => {
      const now = getNow(DEBUG_NOW)
      this.currentWeek = calculateCurrentWeek(now)  // ← 重新计算当前周
      this.selectedWeek = this.currentWeek > 0 ? this.currentWeek : 1
      this.updateWeekDates(now)
      this.loadTodayCourses(now)
      this.loadWeekCourses(now)
      this.loadAllCourses()
    })
    .catch((err) => {
      console.error('Failed to refresh schedule:', err)
    })
}
```

#### refreshSemesterStartFromStorage() 函数（第729-743行）

```javascript
export function refreshSemesterStartFromStorage() {
  return new Promise((resolve, reject) => {
    storage.get({
      key: STORAGE_KEYS.SEMESTER_START,
      success: function (data) {
        semesterWeek1StartCache = data || null  // ← 更新内存缓存
        resolve(semesterWeek1StartCache)
      },
      fail: function (data, code) {
        console.error('Failed to refresh semester start from storage:', code)
        resolve(semesterWeek1StartCache)
      }
    })
  })
}
```

#### updateWeekDates() 函数（第614-649行）

```javascript
updateWeekDates(now) {
  const semesterStart = getSemesterWeek1Start()  // ← 使用最新缓存
  let targetMonday

  if (semesterStart && this.selectedWeek > 0) {
    // 根据学期第一周直接计算目标教学周的周一
    const week1Monday = new Date(semesterStart + 'T00:00:00')
    targetMonday = new Date(week1Monday)
    targetMonday.setDate(targetMonday.getDate() + (this.selectedWeek - 1) * 7)
  } else {
    // 未设置学期第一周时，使用当前自然周作为第1周基准
    // ...
  }

  // 更新 weekDays 的日期和月份
  // ...
}
```

## 五、修改前后逻辑对比

无修改。当前逻辑已经正确：

| 场景 | 行为 | 结果 |
|------|------|------|
| 修改第一周后返回 | onShow → refreshSemesterStartFromStorage → calculateCurrentWeek | 周数正确更新 |
| 未修改设置返回 | onShow → refreshSemesterStartFromStorage（无变化） | 正常显示 |
| 应用重启 | onInit → 读取 Storage | 读取保存的设置 |

## 六、测试结果

### 测试1：修改第一周

**步骤**：
1. 打开课表
2. 查看当前周
3. 进入设置
4. 修改第一周日期
5. 保存
6. 返回课表

**预期**：课表立即刷新

**分析**：onShow 会调用 refreshSemesterStartFromStorage()，然后重新计算 currentWeek

### 测试2：多次切换页面

**步骤**：课表 → 设置 → 课表，重复5次

**预期**：每次返回都正确显示

**分析**：每次 onShow 都会刷新数据

### 测试3：应用重新启动

**预期**：读取保存的第一周设置

**分析**：onInit 会从 Storage 读取数据

## 七、性能影响分析

当前设计性能良好：

| 方面 | 分析 |
|------|------|
| Storage 读取 | 每次 onShow 读取一次，频率合理 |
| 计算开销 | calculateCurrentWeek 轻量，无性能问题 |
| UI 更新 | 必要的刷新，不会造成卡顿 |

## 八、发现但未修改的问题

无。

## 九、后续建议

1. **当前设计已经最优**：不需要修改
2. **数据同步**：确保所有页面的 onShow 都正确刷新依赖的数据
3. **用户体验**：当前实现已经支持即时刷新，用户体验良好
