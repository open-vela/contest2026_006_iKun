# 默认数据开关缓存同步调试报告

时间：2026-09-08 11:32

## 一、问题原因

经过检查，setUseMockData 函数已经正确更新了 useMockDataCache：

```javascript
export function setUseMockData(useMock) {
  return new Promise((resolve, reject) => {
    useMockDataCache = useMock  // 已经更新缓存
    storage.set({
      key: STORAGE_KEYS.DEBUG_USE_MOCK_DATA,
      value: String(useMock),
      // ...
    })
  })
}
```

但是问题仍然存在，需要通过日志进一步定位。

## 二、修改文件

| 文件 | 修改内容 |
|------|----------|
| `src/common/storage.js` | 添加调试日志 |

## 三、缓存同步逻辑

### setUseMockData 函数

```javascript
export function setUseMockData(useMock) {
  return new Promise((resolve, reject) => {
    useMockDataCache = useMock
    console.log('[DEBUG] setUseMockData cache updated:', useMockDataCache)
    storage.set({
      key: STORAGE_KEYS.DEBUG_USE_MOCK_DATA,
      value: String(useMock),
      success: function () {
        console.log('Use mock data setting saved:', useMock)
        resolve()
      },
      fail: function (data, code) {
        console.error('Failed to save use mock data setting:', code)
        reject(new Error('Failed to save setting'))
      }
    })
  })
}
```

### getCourses/getExams/getTasks 函数

```javascript
export function getExams() {
  console.log('[DEBUG] getExams useMockDataCache:', useMockDataCache)
  if (useMockDataCache) {
    // 合并默认数据和用户数据，去重
    const userExamIds = examCache.map(e => e.id)
    const mockExams = exams.filter(e => !userExamIds.includes(e.id))
    return [...mockExams, ...examCache]
  }
  return examCache
}
```

## 四、测试日志

请按以下步骤测试，查看控制台日志：

### 测试1：关闭默认数据

1. 打开应用
2. 进入Debug页面
3. 关闭默认演示数据
4. 返回首页

**预期日志**：
```
[DEBUG] setUseMockData cache updated: false
[STORAGE_SET] key=debug_use_mock_data,value=false
...
[DEBUG] getExams useMockDataCache: false
[DEBUG] getTasks useMockDataCache: false
[PERF:EXAM] getExamSummary_END (count: 0)
[PERF:TASK] getTaskSummary_END (total: 0)
```

### 测试2：开启默认数据

1. Debug开启默认演示数据
2. 返回首页

**预期日志**：
```
[DEBUG] setUseMockData cache updated: true
[STORAGE_SET] key=debug_use_mock_data,value=true
...
[DEBUG] getExams useMockDataCache: true
[DEBUG] getTasks useMockDataCache: true
[PERF:EXAM] getExamSummary_END (count: 3)
[PERF:TASK] getTaskSummary_END (total: 7)
```

## 五、测试结果

请运行测试并告诉我：
1. `[DEBUG] setUseMockData cache updated:` 的值是什么
2. `[DEBUG] getExams useMockDataCache:` 的值是什么
3. `[PERF:EXAM] getExamSummary_END (count:` 的值是什么

## 六、影响范围

| 影响项 | 说明 |
|--------|------|
| storage.js | 添加调试日志 |
| 其他文件 | 无修改 |
